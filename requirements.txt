# KOA（Python 包 koa）：关节间隙（JWD 数值 / JSN 评估）、硬化、骨刺可视化、jsn_eval
# 建议：Python 3.10–3.12（项目内笔记本曾使用 Python 3.11）
# 安装：conda activate <你的环境> && pip install -r requirements.txt
#
# 说明：本文件不包含 PyTorch / nnUNet。仅做分割后测量与评估时无需 GPU。
# 若与本机上的 nnU-Net 训练/推理环境共用，可选用同一 Python 版本后合并依赖。

numpy>=1.24,<3
scipy>=1.10
pandas>=2.0
matplotlib>=3.7
opencv-python-headless>=4.8
SimpleITK>=2.3
pydicom>=2.4
scikit-learn>=1.3
openpyxl>=3.1
